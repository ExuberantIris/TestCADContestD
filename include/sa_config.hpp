#pragma once

/** Setup-only longest-path solver parameters. */
namespace SaConfig {

/** Wall-clock budget for setup longest-path solver (seconds). */
inline constexpr double kSetupLpTimeLimitSec = 180.0;

} // namespace SaConfig
